.PHONY = all clean

CC = g++
CFLAGS = -g -Wall -fopenmp

SRCS = $(wildcard *.cpp)
BINS = $(SRCS:%.cpp=%)

all: ${BINS}

%: %.o
	@echo "Checking.."
	${CC} ${CFLAGS} $< -o $@

%.o: %.cpp
	@echo "Creating object.."
	${CC} -c $< 

clean:
	rm -rvf *.o ${BINS}

print: *.cpp
	lpr -p $?
	touch print